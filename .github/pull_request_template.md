## What changes

One mechanism. Before and after, with numbers where there are any.

## How it was checked

Tests added or changed, and any proof outside the suite (a real relay, two machines,
a screenshot).

## Leak check

- [ ] No names, employer, Slack ids or internal app names. `bun scripts/fugas.ts` passes locally.
