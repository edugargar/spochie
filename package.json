{
  "name": "spochie",
  "version": "0.5.2",
  "private": true,
  "description": "A tunnel between two people's Claude Code sessions, with Slack for notifications and the thread",
  "type": "module",
  "bin": {
    "spochie": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "daemon": "bun run src/daemon.ts",
    "spochie": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
